{/* Label.mdx */}
import { Canvas, Meta, Stories, Controls, Description, Story, Source } from '@storybook/blocks';

import * as LabelStories from './Label.stories';

{/* Metaがないと正しく動作しない */}

<Meta title="Label" of={LabelStories} />

# ラベル Label

情報の識別子を付与したり、分類を示す際に利用します。

## 原則

基本的に他のコンポーネントの中に含める形で使用します。その場合のスタイル（サイズバリエーション、リンクやアイコンの有無）はコンポーネントごとの原則に準じます。

<Canvas />
<Controls />

<Stories />
